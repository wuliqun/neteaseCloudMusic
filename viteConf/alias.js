const { resolve } = require('path');

module.exports = {
  "CMT": resolve(__dirname, '../src/components'),
  "UTIL": resolve(__dirname, '../src/utils'),
  "API": resolve(__dirname, '../src/api'),
  "CONFIG": resolve(__dirname, '../src/config')
};